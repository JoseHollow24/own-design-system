import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as ModalStories from "./modal.stories";

<Meta of={ModalStories} />

# dsh-modal

Web component de modal del DSH Design System. Soporta contenido con ícono, imagen promo, avatar y un área segura para contenido adicional. Los botones de acción van en el slot `actionable`.

## Básico

Modal con título, descripción y botones de acción.

<Canvas of={ModalStories.Basic} />

## Con botón de cierre

El prop `closable` muestra el botón "Cerrar" en la parte superior. El modal también puede cerrarse con la tecla `Escape` o haciendo click en el fondo oscuro.

<Canvas of={ModalStories.WithClose} />

## Con ícono

El prop `icon` acepta cualquier nombre de Font Awesome (ej: `fa-circle-check`). El color se controla con `icon-color`.

<Canvas of={ModalStories.WithIcon} />

## Con área segura

El slot `safe-area` agrega contenido adicional bajo el cuerpo del modal, separado visualmente del contenido principal.

<Canvas of={ModalStories.WithSafeArea} />

## Con imagen (promo)

Usando `variant="promo"` junto con `image-src` e `image-alt` se activa la variante con imagen superior y efecto de silueta.

<Canvas of={ModalStories.WithImage} />

## Con avatar

El prop `avatar-src` muestra una imagen de perfil circular sobre el contenido del modal.

<Canvas of={ModalStories.WithAvatar} />

## Playground

<Canvas of={ModalStories.Playground} />
<Controls of={ModalStories.Playground} />

## Props

<table style={{width:'100%',borderCollapse:'collapse',fontSize:'14px',marginBottom:'24px'}}>
  <thead>
    <tr style={{background:'#ebeff4'}}>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Prop</th>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Atributo</th>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Tipo</th>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Default</th>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Descripción</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>open</code></td>
      <td style={{padding:'10px 14px'}}><code>open</code></td>
      <td style={{padding:'10px 14px'}}>boolean</td>
      <td style={{padding:'10px 14px'}}><code>false</code></td>
      <td style={{padding:'10px 14px'}}>Controla si el modal está visible</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>closable</code></td>
      <td style={{padding:'10px 14px'}}><code>closable</code></td>
      <td style={{padding:'10px 14px'}}>boolean</td>
      <td style={{padding:'10px 14px'}}><code>false</code></td>
      <td style={{padding:'10px 14px'}}>Muestra el botón "Cerrar"</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>noClose</code></td>
      <td style={{padding:'10px 14px'}}><code>no-close</code></td>
      <td style={{padding:'10px 14px'}}>boolean</td>
      <td style={{padding:'10px 14px'}}><code>false</code></td>
      <td style={{padding:'10px 14px'}}>Desactiva cierre por fondo y tecla Escape</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>fixed</code></td>
      <td style={{padding:'10px 14px'}}><code>fixed</code></td>
      <td style={{padding:'10px 14px'}}>boolean</td>
      <td style={{padding:'10px 14px'}}><code>false</code></td>
      <td style={{padding:'10px 14px'}}>Modal de altura completa (grid layout)</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>textTitle</code></td>
      <td style={{padding:'10px 14px'}}><code>text-title</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>Título del modal</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>textDescription</code></td>
      <td style={{padding:'10px 14px'}}><code>text-description</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>Descripción o cuerpo del modal</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>icon</code></td>
      <td style={{padding:'10px 14px'}}><code>icon</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>Nombre del ícono FA (ej: <code>fa-circle-check</code>)</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>iconColor</code></td>
      <td style={{padding:'10px 14px'}}><code>icon-color</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}><code>x1</code></td>
      <td style={{padding:'10px 14px'}}>Color del ícono</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>imageSrc</code></td>
      <td style={{padding:'10px 14px'}}><code>image-src</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>URL de la imagen (requiere <code>variant="promo"</code>)</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>imageAlt</code></td>
      <td style={{padding:'10px 14px'}}><code>image-alt</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>Texto alternativo de la imagen</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>avatarSrc</code></td>
      <td style={{padding:'10px 14px'}}><code>avatar-src</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>URL del avatar circular</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>avatarAlt</code></td>
      <td style={{padding:'10px 14px'}}><code>avatar-alt</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>Texto alternativo del avatar</td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>variant</code></td>
      <td style={{padding:'10px 14px'}}><code>variant</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>Variante del card: <code>promo</code></td>
    </tr>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>actionableAlign</code></td>
      <td style={{padding:'10px 14px'}}><code>actionable-align</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}><code>right</code></td>
      <td style={{padding:'10px 14px'}}>Alineación del footer: <code>left</code>, <code>center</code>, <code>right</code></td>
    </tr>
    <tr>
      <td style={{padding:'10px 14px'}}><code>id</code></td>
      <td style={{padding:'10px 14px'}}><code>id</code></td>
      <td style={{padding:'10px 14px'}}>string</td>
      <td style={{padding:'10px 14px'}}>—</td>
      <td style={{padding:'10px 14px'}}>ID base del modal (se sufija <code>-title</code> y <code>-description</code> para aria)</td>
    </tr>
  </tbody>
</table>

## Slots

<table style={{width:'100%',borderCollapse:'collapse',fontSize:'14px',marginBottom:'24px'}}>
  <thead>
    <tr style={{background:'#ebeff4'}}>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Slot</th>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Descripción</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{borderBottom:'1px solid #dfe3e5'}}>
      <td style={{padding:'10px 14px'}}><code>actionable</code></td>
      <td style={{padding:'10px 14px'}}>Botones de acción en el footer del modal</td>
    </tr>
    <tr>
      <td style={{padding:'10px 14px'}}><code>safe-area</code></td>
      <td style={{padding:'10px 14px'}}>Contenido adicional bajo el cuerpo principal</td>
    </tr>
  </tbody>
</table>

## Eventos

<table style={{width:'100%',borderCollapse:'collapse',fontSize:'14px'}}>
  <thead>
    <tr style={{background:'#ebeff4'}}>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Evento</th>
      <th style={{padding:'10px 14px',textAlign:'left',borderBottom:'2px solid #c9d0d3'}}>Descripción</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style={{padding:'10px 14px'}}><code>close</code></td>
      <td style={{padding:'10px 14px'}}>Emitido al cerrar el modal (fondo, botón Cerrar o Escape)</td>
    </tr>
  </tbody>
</table>
